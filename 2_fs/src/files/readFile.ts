import fs from "fs";

export const readFile = async () => {
    // fs.mkdir('subfolder', (err) => console.log(err));

    // fs.readFile('./text.txt')
    //     .then((data) => {
    //         console.log(data.toString());
    //     })
    //     .catch((err) => {
    //         console.log(`Error: ${err}`);
    //     });
};